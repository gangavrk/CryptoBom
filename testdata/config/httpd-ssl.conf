# Apache httpd TLS config sample. +/- enable/disable semantics: only the ENABLED
# weak protocol should be flagged; the disabled ones must NOT be.
SSLProtocol -all +TLSv1.1 +TLSv1.2
SSLProtocol all -SSLv3 -TLSv1
SSLCipherSuite HIGH:!aNULL:!MD5
