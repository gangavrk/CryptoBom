# nginx TLS config sample for cryptobom regression testing.
server {
    listen 443 ssl;
    # TLSv1 and TLSv1.1 are deprecated (flagged); TLSv1.2/1.3 are inventory.
    ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_certificate /etc/nginx/cert.pem;
}
